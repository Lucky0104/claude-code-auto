# Root env template. See apps/web/.env.example and workers/*/.dev.vars.example
# for the full per-package variable lists.
#
# Prisma (used by `npm run db:*` at the repo root) needs:
DATABASE_URL=postgresql://postgres:password@db.YOUR-PROJECT.supabase.co:6543/postgres?pgbouncer=true
DIRECT_URL=postgresql://postgres:password@db.YOUR-PROJECT.supabase.co:5432/postgres
